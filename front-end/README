Iniciando:

criar o projeot com vite:

npm create vite@latest

irá perguntar se deseja instalar o create-vite, y para aceitar Need to install the following packages: create-vite@4.3.2 Ok to proceed? (y) y

selecina o nome do projeto √ Project name: ... Notiworks

nome da pasta √ Package name: ... notiworks

qual o framework √ Select a framework: » React

e o tipo do js √ Select a variant: » TypeScript

Com isso ele cria o projeto na pasta desejada, movemos para pasta e executamos para iniciar o projeto.

cd Notiworks
npm install 
npm install axios 
npm install react-router-dom
bonpm install @mui/material @emotion/react @emotion/styled
npm install @mui/icons-material @mui/material @emotion/styled @emotion/react
npm run dev